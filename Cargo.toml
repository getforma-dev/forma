[workspace]
members = ["crates/forma-ir", "crates/forma-server"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

# Local dev overrides — forma-server resolves forma-ir from this workspace
[patch.crates-io]
forma-ir = { path = "crates/forma-ir" }
